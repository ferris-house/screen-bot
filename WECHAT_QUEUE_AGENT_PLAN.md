# 企业微信队列推送改造方案

## 目标

将当前 Electron 客户端改造成一个本地企业微信推送 Agent：

- 用户启动客户端后，配置一个公网推送队列接口。
- 客户端定时轮询该接口领取待发送任务。
- 客户端在本机操作已登录的企业微信，把任务消息发送到指定群聊。
- 客户端将每个任务的执行结果回传给队列服务。

该方案的核心是“云端或公网接口负责管理任务，本地客户端负责执行桌面自动化”。

## 推荐架构

```text
外部业务系统
   |
   v
公网推送队列接口
   |
   | 客户端定时轮询 / claim 任务
   v
Electron 客户端
   |
   | robotjs / node-window-manager / 剪贴板 / 快捷键
   v
本机企业微信客户端
```

不建议让云服务器直接运行企业微信桌面自动化。普通云服务器没有稳定桌面环境，登录态、图形界面、分辨率、远程会话和风控都很难长期稳定维护。

## 阶段 1：定义任务协议

客户端不要自行识别所有群，而是由接口返回明确的群名和消息内容。

推荐任务结构：

```json
{
  "id": "task_001",
  "targets": ["销售一群", "售后通知群"],
  "message": "今天 18:00 前提交日报",
  "type": "text",
  "dedupeKey": "notice-001"
}
```

推荐接口：

```http
POST /tasks/claim
POST /tasks/:id/result
GET /health
```

使用 `claim` 接口而不是普通查询接口，是为了让服务端能把任务从 `pending` 改为 `processing`，避免多个客户端重复领取同一个任务。

## 阶段 2：增加客户端配置

在客户端界面中增加远程队列配置：

- 推送队列接口 URL
- Token / Secret
- 轮询间隔，默认 60 秒
- 是否启用自动轮询
- 当前连接状态
- 最近一次拉取时间
- 最近一次错误信息

早期版本可以继续使用 `localStorage` 保存配置。后续如果要支持更完整的客户端设置，可以迁移到 Electron 主进程管理的本地配置文件。

## 阶段 3：主进程增加轮询调度

轮询和自动化执行建议放在 `main.js`，不要放在 `renderer.js`。

原因：

- 主进程更适合控制 `robotjs`、窗口、剪贴板。
- 界面刷新不应该打断正在执行的任务。
- 后续打包、权限检测和日志管理更清晰。

推荐任务状态机：

```text
idle
  -> polling
  -> claimed
  -> sending
  -> reporting
  -> idle
```

同一时间只执行一个任务。企业微信 GUI 自动化不适合并发执行。

## 阶段 4：改造发送逻辑

当前项目已有基础发送能力：

```text
等待微信窗口激活
写入剪贴板
模拟粘贴
模拟发送
```

后续需要增强为按群名发送：

```text
激活企业微信
打开搜索
输入群名
进入群聊
粘贴消息
发送
记录结果
进入下一个群
```

优先使用快捷键和剪贴板实现，不要一开始就依赖图像识别。

建议策略：

1. 激活企业微信窗口。
2. 使用快捷键打开搜索。
3. 输入群名。
4. 回车进入群聊。
5. 将消息写入剪贴板。
6. 粘贴消息。
7. 使用快捷键发送。
8. 记录该群的发送结果。

图像识别可以后续作为辅助能力，用于确认搜索框、聊天输入区、发送失败状态等。

## 阶段 5：回传任务结果

任务执行完成后，客户端需要回传完整结果，而不是只回传成功或失败。

示例：

```json
{
  "taskId": "task_001",
  "status": "partial_success",
  "results": [
    {
      "target": "销售一群",
      "status": "sent"
    },
    {
      "target": "售后通知群",
      "status": "failed",
      "error": "未找到群"
    }
  ],
  "startedAt": "2026-06-04T10:00:00.000Z",
  "finishedAt": "2026-06-04T10:01:20.000Z"
}
```

群发任务中，“部分成功”是正常状态，需要被协议支持。

## 阶段 6：容错机制

需要重点处理以下异常：

- 客户端断网
- 队列接口超时
- 企业微信未打开
- 企业微信未登录
- 当前桌面锁屏
- 群名搜索不到
- 消息粘贴失败
- 发送快捷键无效
- 任务执行到一半客户端退出

服务端任务建议支持租约字段：

```json
{
  "leaseExpiresAt": "2026-06-04T10:10:00.000Z"
}
```

客户端领取任务后，如果长时间未回传结果，服务端可以在租约过期后重新释放任务。

## 阶段 7：安全设计

用户填写的公网接口至少需要：

- HTTPS
- Bearer Token
- 请求超时
- 任务大小限制
- 消息长度限制
- 本地日志脱敏

如果后续建设自有云端服务，建议进一步增加：

- 签名校验
- 防重放
- IP 限流
- 用户和 Agent 绑定
- 任务审计

## 阶段 8：权限和启动检测

客户端启动后应展示基础状态检查：

```text
企业微信：已检测 / 未检测
辅助功能权限：已授权 / 未授权
屏幕录制权限：已授权 / 未授权
队列接口：连接正常 / 连接失败
自动轮询：开启 / 关闭
```

macOS 上通常需要辅助功能权限。若后续加入截图或图像识别，还可能需要屏幕录制权限。这些权限无法完全绕过，应在客户端内提供清晰引导。

## 阶段 9：是否引入图像识别

当前项目已经使用：

- `robotjs`
- `node-window-manager`
- Electron 剪贴板能力

建议 MVP 阶段先不引入复杂图像识别，优先依赖：

- 窗口标题识别
- 快捷键搜索群
- 剪贴板粘贴
- 回车或发送快捷键

等远程队列和基础群发送跑通后，再考虑补充：

- 截图能力
- 模板图片匹配
- 搜索框状态确认
- 输入区状态确认
- 失败截图留证

## 阶段 10：打包和分发

项目已经配置 `electron-builder`，并已包含 `robotjs` 相关 rebuild 脚本。后续需要重点验证：

- Windows 打包
- macOS 打包
- 原生依赖 rebuild
- 安装后权限申请
- 开机自启，可选
- 自动更新，可选
- 日志文件导出
- 崩溃恢复

## 推荐实施顺序

1. 增加远程队列配置页：接口 URL、Token、轮询开关。
2. 在主进程实现定时轮询和任务领取。
3. 将远程任务转换为当前已有的发送调用。
4. 增强为按群名搜索并发送。
5. 增加任务执行结果回传。
6. 增加失败重试、租约超时、任务幂等。
7. 增加客户端状态检测和运行日志。
8. 根据实际稳定性再补充图像识别和截图留证。

## 结论

该项目的最小改造路径是：保留现有桌面发送能力，新增远程任务轮询和任务状态系统。

当前阶段不建议优先引入复杂图像识别。应先把“客户端轮询任务、领取任务、按群名发送、回传结果”跑通，再逐步增强识别、容错和审计能力。
